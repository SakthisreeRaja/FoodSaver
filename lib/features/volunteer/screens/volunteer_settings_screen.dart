import 'package:foodsaver/core/screens/app_settings_screen.dart';
import 'package:flutter/material.dart';

class VolunteerSettingsScreen extends StatelessWidget {
  const VolunteerSettingsScreen({super.key});
  @override
  Widget build(BuildContext context) => const AppSettingsScreen(role: 'volunteer');
}
